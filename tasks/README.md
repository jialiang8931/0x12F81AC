# Tasks

這裡只保存已核准的 bounded work。每個 task 必須列 owner、objective、providers、consumers、writable paths、forbidden paths、verification、evidence path 與 rollback。

初始化期間，task 編號與 `taskN/<english-purpose>` branch 對應。完成必要 gate 並合回 `dev` 後，才標成 `DONE`。未核准提案不得變成 task。
