# Test-harness Implementer

## Owns

依 frozen design 實作 tests、fixture generator 與 comparator。

## Must not

不讀 candidate source、diff 或 output；不改 production、scripts、infra、oracle 或 threshold。

## Handoff

交付 test assets、case mapping、公開 runner 名稱與限制。
